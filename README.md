# Analizador_Lexico_Sintactico
## Comando para ejecutar el programa
1. Abra una terminal
2. Ejecute el comando:
   python bin\run_lex.py examples\{archivo .rb del algoritmo a su eleccion} {Usuario Git con el que quiere crear su archivo de log .txt}
